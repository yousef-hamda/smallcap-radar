import ts from 'typescript';import fs from 'node:fs/promises';import{spawnSync}from'node:child_process';
await fs.mkdir('.test-build',{recursive:true});for(const name of ['engine','fixtures','research','sec','providers']){const source=await fs.readFile(`lib/${name}.ts`,'utf8');let js=ts.transpileModule(source,{compilerOptions:{target:ts.ScriptTarget.ES2022,module:ts.ModuleKind.ESNext}}).outputText;js=js.replace(/from ['"]\.\/([^'"]+)['"]/g,"from './$1.mjs'");await fs.writeFile(`.test-build/${name}.mjs`,js)}const r=spawnSync(process.execPath,['--test','tests/engine/strategy.test.mjs'],{stdio:'inherit'});process.exitCode=r.status;
