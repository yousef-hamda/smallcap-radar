"use client";
/* The refresh function intentionally tracks the current strategy view and is
   called from controlled polling effects; including it as a dependency would
   recreate the polling timer on every render. */
/* eslint-disable react-hooks/exhaustive-deps */

import {useEffect,useMemo,useState} from "react";
import Link from "next/link";
import {
 Activity,AlertTriangle,ArrowUpLeft,BarChart3,Bell,Check,ChevronLeft,Clock3,
 Database,Download,FileUp,FlaskConical,Info,Layers,Menu,Radar,RefreshCw,
 Search,Server,ShieldCheck,Star,Wifi,X
} from "lucide-react";
import {Dialog,DialogContent,DialogDescription,DialogTitle} from "@/components/ui/dialog";
import {Sheet,SheetContent,SheetDescription,SheetTitle} from "@/components/ui/sheet";
import {Progress} from "@/components/ui/progress";
import {evaluateStrategy,SPECS,type Snapshot} from "@/lib/engine";
import {fixtures} from "@/lib/fixtures";
import {simulateExit} from "@/lib/research";
import {saveOffline} from "@/lib/offline";
import PriceChart from "./price-chart";

const money=(n:number|null|undefined)=>n==null?'—':new Intl.NumberFormat('en-US',{notation:'compact',maximumFractionDigits:2}).format(n);
const pct=(n:number|null|undefined)=>n==null?'—':`${(n*100).toFixed(1)}%`;
const labels:Record<string,string>={PASS:'اجتاز الفحص',FAIL:'مستبعد',UNKNOWN:'يحتاج تحقق'};
const oldCheckLabels:Record<string,string>={security:'الإدراج',cap:'الحجم',liquidity:'السيولة',collapse:'عمق الهبوط',low:'خرج من القاع',dilution:'لا تخفيف حاد',reversal:'انعكاس مؤكد',revenue:'قاعدة الإيراد والتقييم',valuation:'التقييم (ق.م/مبيعات + عائد التدفق)',profitability:'الربحية أو الانعطاف',deathSpiral:'لا سبيل موت',provenance:'المصادر والتوقيت',freshness:'حداثة البيانات',filingFreshness:'حداثة الفترة المالية',conflict:'تعارض المصادر'};
const oldFactorLabels:Record<string,string>={valuation:'التقييم',quality:'الجودة والربحية',shareDiscipline:'انضباط الأسهم',sizeCoverage:'الصِغَر',growth:'النمو',insider:'المطّلعون',marginTrend:'الهامش',entry:'نقطة الدخول',balance:'الميزانية'};
const oldScoreLabel=(score:number|null|undefined,strategy:'core'|'bounce')=>{if(score==null)return 'غير معروف';if(strategy==='bounce')return score>=75?'ارتداد قوي':score>=62?'ارتداد جيد':'ارتداد ضعيف';return score>=68?'مرشّح قوي':score>=50?'قائمة مراقبة':'تجاهل'};
const checkMark=(status:string)=>status==='PASS'?'✓':status==='FAIL'?'✕':'~';
const dateOnly=(value?:string|null)=>value?value.slice(0,10):'غير متاح';
const tabTitles:Record<string,string>={home:'لوحة الفرص',core:'القيمة الأساسية',bounce:'فرص الارتداد',favorites:'قائمة المتابعة',lab:'مختبر الاستراتيجية',data:'مركز البيانات'};
const stageLabels:Record<number,string>={0:'تجهيز الجولة',1:'مزامنة الأسعار الجماعية',2:'تنظيف أنواع الأوراق',3:'فلترة الحجم والسيولة',4:'جمع SEC Frames',9:'التقييم المحلي',13:'حفظ النتائج'};
const navItems=[['home','لوحة الفرص',BarChart3],['core','القيمة الأساسية',ShieldCheck],['bounce','فرص الارتداد',Activity],['favorites','قائمة المتابعة',Star],['lab','مختبر الاستراتيجية',FlaskConical],['data','مركز البيانات',Database]] as const;

export default function RadarApp(){
 const [tab,setTab]=useState('bounce');
 const [strategy,setStrategy]=useState<'core'|'bounce'>('bounce');
 const [query,setQuery]=useState('');
 const [data,setData]=useState<Snapshot[]>([]);
 const [radarSummary,setRadarSummary]=useState<any>({total:0,coreQualified:0,bounceQualified:0,coreUnknown:0,bounceUnknown:0,coreFailed:0,bounceFailed:0});
 const [demo,setDemo]=useState(false);
 const [run,setRun]=useState<any>(null);
 const [busy,setBusy]=useState(false);
 const [error,setError]=useState('');
 const [notice,setNotice]=useState('');
 const [selected,setSelected]=useState<Snapshot|null>(null);
 const [favorites,setFavorites]=useState<string[]>([]);
 const [install,setInstall]=useState<any>(null);
 const [installHelp,setInstallHelp]=useState(false);
 const [scanMenu,setScanMenu]=useState(false);
 const [mobileNav,setMobileNav]=useState(false);
 const [lab,setLab]=useState<any>(null);
 const [storedEvaluations,setStoredEvaluations]=useState<any[]>([]);
 const [notificationsEnabled,setNotificationsEnabled]=useState(false);
 const [notificationState,setNotificationState]=useState<'checking'|'ready'|'unsupported'|'denied'>('checking');
 const [detailLoading,setDetailLoading]=useState(false);

 const refresh=async(view:'core'|'bounce'|'favorites'=strategy)=>{const response=await fetch(`/api/radar?strategy=${view}&limit=150`);const payload=await response.json();if(!response.ok)throw Error(payload.error||'تعذّر تحميل آخر فحص.');setData(payload.snapshots||[]);setStoredEvaluations(payload.storedEvaluations||[]);setRadarSummary(payload.summary||{});setRun(payload.run);setFavorites(payload.favorites||[]);setDemo(false);if(payload.dataRunId&&payload.run?.id!==payload.dataRunId)setNotice('نعرض آخر لقطة تحتوي بيانات؛ الجولة الأحدث لم تنتج نتائج بعد.');await saveOffline({savedAt:new Date().toISOString(),run:payload.dataRun,snapshots:(payload.snapshots||[]).map((s:any)=>({...s,history:undefined}))}).catch(()=>{});};

 useEffect(()=>{queueMicrotask(()=>refresh('bounce').catch(e=>setNotice(e.message||'لا توجد لقطة محفوظة بعد.')));const handler=(event:any)=>{event.preventDefault();setInstall(event)};window.addEventListener('beforeinstallprompt',handler);if(!('serviceWorker'in navigator)||!('PushManager'in window)||!('Notification'in window))queueMicrotask(()=>setNotificationState('unsupported'));else navigator.serviceWorker.register('/sw.js').then(registration=>registration.pushManager.getSubscription().then(subscription=>{setNotificationsEnabled(!!subscription);setNotificationState(Notification.permission==='denied'?'denied':'ready')})).catch(()=>setNotificationState('unsupported'));return()=>window.removeEventListener('beforeinstallprompt',handler)},[]);

 useEffect(()=>{if(!run||!['running','partial'].includes(run.status)||(run.offset>=run.total&&!run.retryPending))return;let active=true;let busyStep=false;const tick=async()=>{if(!active||busyStep)return;busyStep=true;try{await fetch('/api/scan',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'step',runId:run.id})});await refresh()}catch{}finally{busyStep=false}};void tick();const timer=window.setInterval(tick,2500);return()=>{active=false;window.clearInterval(timer)}},[run]);

 async function scan(mode:'quick'|'full'){
  setBusy(true);setError('');setNotice('');setDemo(false);setScanMenu(false);
  try{
   const response=await fetch('/api/background-scan/start',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({mode})});
   const payload=await response.json();if(!response.ok)throw Error(payload.error||'تعذّر بدء الفحص.');setRun(payload.run);
   setNotice(notificationsEnabled?'بدأ الفحص على الخادم. يمكنك إغلاق التطبيق وسيصلك إشعار عند انتهائه.':'بدأ الفحص على الخادم وسيستمر بعد إغلاق التطبيق. فعّل الإشعارات ليصلك تنبيه عند الانتهاء.');
  }catch(e:any){setError(e.message||'تعذّر إكمال الفحص.')}finally{setBusy(false)}
 }

 async function enableNotifications(){
  setError('');setNotice('');
  try{
   if(!('serviceWorker'in navigator)||!('PushManager'in window)||!('Notification'in window))throw Error('هذا المتصفح لا يدعم إشعارات الويب. على iPhone ثبّت الموقع على الشاشة الرئيسية وافتحه من هناك.');
   const standalone=window.matchMedia('(display-mode: standalone)').matches||(navigator as any).standalone===true;
   if(/iPhone|iPad|iPod/i.test(navigator.userAgent)&&!standalone)throw Error('على iPhone: أضف الموقع إلى الشاشة الرئيسية أولًا، ثم افتحه من الأيقونة وفعّل الإشعارات.');
   const permission=await Notification.requestPermission();
   if(permission!=='granted')throw Error('لم يتم السماح بالإشعارات. يمكنك تفعيلها من إعدادات الهاتف.');
   const registration=await navigator.serviceWorker.ready;
   const keyResponse=await fetch('/api/push/key');const keyPayload=await keyResponse.json();if(!keyResponse.ok||!keyPayload.publicKey)throw Error('تعذّر تجهيز مفتاح الإشعارات.');
   const bytes=Uint8Array.from(atob(keyPayload.publicKey.replace(/-/g,'+').replace(/_/g,'/').padEnd(Math.ceil(keyPayload.publicKey.length/4)*4,'=')),character=>character.charCodeAt(0));
   const subscription=await registration.pushManager.getSubscription()||await registration.pushManager.subscribe({userVisibleOnly:true,applicationServerKey:bytes});
   const response=await fetch('/api/push/subscribe',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(subscription)});const payload=await response.json();if(!response.ok)throw Error(payload.error||'تعذّر حفظ اشتراك الإشعارات.');
   setNotificationsEnabled(true);setNotificationState('ready');
   const testResponse=await fetch('/api/push/test',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({endpoint:subscription.endpoint})});const testPayload=await testResponse.json();if(!testResponse.ok)throw Error(testPayload.error||'تم الاشتراك لكن تعذّر إرسال إشعار الاختبار.');
   setNotice('تم التفعيل وإرسال إشعار اختبار إلى هذا الهاتف. سيصلك تنبيه آخر عند انتهاء الفحص.');
  }catch(e:any){if(typeof Notification!=='undefined'&&Notification.permission==='denied')setNotificationState('denied');setError(e.message||'تعذّر تفعيل الإشعارات.')}
 }

 async function favorite(symbol:string){if(demo){setNotice('الحفظ متاح للبيانات الفعلية فقط.');return}try{const response=await fetch('/api/radar',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'favorite',symbol})});const payload=await response.json();if(!response.ok)throw Error(payload.error||'تعذّر حفظ الشركة.');setFavorites(payload.favorites)}catch(e:any){setError(e.message)}}
 async function openCompany(snapshot:Snapshot){setSelected(snapshot);if(demo)return;setDetailLoading(true);try{const response=await fetch(`/api/company?symbol=${encodeURIComponent(snapshot.symbol)}`);const payload=await response.json();if(!response.ok)throw Error(payload.error||'تعذّر تحميل التحقق العميق.');setSelected(payload.snapshot)}catch(e:any){setNotice(`تعذّر تحديث التفاصيل: ${e.message}`)}finally{setDetailLoading(false)}}
 async function importFile(file?:File){if(!file)return;setBusy(true);setError('');try{if(file.size>4_000_000)throw Error('الحد الأقصى للملف 4 MB.');const records=JSON.parse(await file.text());const response=await fetch('/api/radar',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'import',records})});const payload=await response.json();if(!response.ok)throw Error(payload.error);await refresh();setNotice('تم استيراد اللقطة وتقييمها بالقواعد الحالية.')}catch(e:any){setError(e.message)}finally{setBusy(false)}}

 const evaluated=useMemo(()=>data.map(snapshot=>({s:snapshot,e:evaluateStrategy(strategy,snapshot)})),[data,strategy]);
 const rows=useMemo(()=>{const source=tab==='favorites'?data.flatMap(s=>{const bounce=evaluateStrategy('bounce',s) as any;const core=evaluateStrategy('core',s) as any;return [{s,e:bounce.screeningQualified?bounce:core}]}):evaluated;return source.filter(({s,e})=>(tab==='favorites'?favorites.includes(s.symbol):(e as any).screeningQualified)&&(!query||`${s.symbol} ${s.name}`.toLowerCase().includes(query.toLowerCase()))).sort((a,b)=>a.s.symbol.localeCompare(b.s.symbol))},[evaluated,data,query,tab,favorites]);
 const screeningQualified=strategy==='core'?radarSummary.coreQualified:radarSummary.bounceQualified;
 const incomplete=strategy==='core'?radarSummary.coreUnknown:radarSummary.bounceUnknown;
 const excluded=strategy==='core'?radarSummary.coreFailed:radarSummary.bounceFailed;
 const mobileBounceCount=radarSummary.bounceQualified;
 const mobileCoreCount=radarSummary.coreQualified;
 const progress=run?.total?Math.min(100,100*(run.offset||0)/run.total):0;
 const resumable=run&&run.source?.includes('Bulk Quotes/SEC Frames v7')&&['running','partial'].includes(run.status)&&(run.stage<13||run.offset<run.total||run.retryPending);

 function switchTab(id:string){setTab(id);setMobileNav(false);const view:'core'|'bounce'|'favorites'=id==='core'||id==='bounce'||id==='favorites'?id as 'core'|'bounce'|'favorites':'bounce';if(id==='core'||id==='bounce')setStrategy(id);void refresh(view).catch(e=>setError(e.message||'تعذّر تبديل القائمة.'))}
 function exportData(){const blob=new Blob([JSON.stringify({run,mode:demo?'synthetic':'observed',strategy:SPECS[strategy],results:evaluated,evaluationMode:'current-spec reevaluation',persistedEvaluations:demo?[]:storedEvaluations},null,2)],{type:'application/json'});const url=URL.createObjectURL(blob),anchor=document.createElement('a');anchor.href=url;anchor.download='small-cap-radar-audit.json';anchor.click();URL.revokeObjectURL(url)}

 return <div className="terminal-shell" dir="rtl">
  <aside className={`sidebar ${mobileNav?'open':''}`}>
   <div className="sidebar-head"><Link className="wordmark" href="/"><span className="mark"><Radar size={21}/></span><span><b>رادار</b><small>SMALL CAP INTELLIGENCE</small></span></Link><button className="mobile-close" onClick={()=>setMobileNav(false)} aria-label="إغلاق القائمة"><X size={20}/></button></div>
   <div className="nav-caption">مساحة العمل</div>
   <nav>{navItems.map(([id,name,Icon])=><button key={id} className={tab===id?'side-link active':'side-link'} onClick={()=>switchTab(id)}><Icon size={18}/><span>{name}</span>{tab===id&&<ChevronLeft size={15}/>}</button>)}</nav>
   <div className="sidebar-status"><div className="status-line"><span className="live-dot"/><b>جاهز للفحص</b></div><p>النتائج محفوظة ويمكنك العودة إليها في أي وقت.</p><button className="install-link" onClick={()=>install?install.prompt():setInstallHelp(true)}><Download size={16}/> تثبيت التطبيق</button></div>
  </aside>
  {mobileNav&&<button className="nav-scrim" onClick={()=>setMobileNav(false)} aria-label="إغلاق القائمة"/>}

  <main className="app-main">
   <header className="appbar"><button className="menu-button" onClick={()=>setMobileNav(true)} aria-label="فتح القائمة"><Menu size={21}/></button><div className="breadcrumb"><span>رادار</span><ChevronLeft size={14}/><b>{tabTitles[tab]}</b></div><div className="appbar-meta"><span className="market-state"><span className="live-dot"/> البيانات متصلة</span><span className="avatar">YH</span></div></header>
   <div className="content">
    <section className="command-header"><div><span className="kicker">نظرة عامة</span><h1>{tabTitles[tab]}</h1><p>{tab==='home'?'اكتشف الشركات التي تستحق التحقق بوضوح وسرعة.':tab==='lab'?'اختبر القواعد والتكاليف قبل اعتماد أي نتيجة.':tab==='data'?'راقب تغطية كل مصدر وحالة البيانات.':'القرار أولًا، والتفاصيل متاحة عند الحاجة.'}</p></div><div className="scan-actions"><button className="scan-primary" onClick={()=>resumable?scan(run.source?.includes('quick')?'quick':'full'):setScanMenu(v=>!v)} disabled={busy}><RefreshCw size={17} className={busy||resumable?'spin':''}/>{busy?'جارٍ البدء':resumable?'الفحص يعمل':'فحص السوق'}</button>{scanMenu&&<div className="scan-popover"><button onClick={()=>scan('full')}><b>فحص السوق الكامل</b><span>كل السوق، ثم تحليل المرشحين فقط</span></button><button onClick={()=>scan('quick')}><b>فحص سريع</b><span>عينة من 12 سهمًا للتجربة</span></button></div>}</div></section>
    <section className="mobile-command" aria-label="رادار الشركات الصغيرة"><div className="mobile-command-title"><h1>رادار الشركات الصغيرة</h1><p>{(radarSummary.total||run?.universe_total||0).toLocaleString('en-US')} شركة مفحوصة · —</p></div><div className="mobile-command-actions"><button className="mobile-scan" onClick={()=>scan(resumable?(run.source?.includes('quick')?'quick':'full'):'full')} disabled={busy||resumable}>{busy?'جارٍ البدء':resumable?'الفحص يعمل':'فحص السوق'}</button><button className="mobile-favorites" onClick={()=>switchTab('favorites')} aria-label="قائمة المتابعة"><Star size={19} fill={tab==='favorites'?'currentColor':'none'}/><b>{favorites.length}</b></button></div></section>
    <div className="mobile-search"><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="إبحث برمز السهم أو اسم الشركة" aria-label="بحث برمز السهم أو اسم الشركة"/><button disabled={!query.trim()}>بحث</button></div>

    {(error||notice||demo)&&<div className={`system-message ${error?'danger':demo?'synthetic':''}`}>{error?<AlertTriangle size={17}/>:demo?<FlaskConical size={17}/>:<Info size={17}/>}<span>{error||notice||(demo?'وضع العرض التجريبي: البيانات اصطناعية ولا تمثل شركات حقيقية.':'')}</span>{demo&&<button onClick={()=>refresh().catch(e=>setError(e.message))}>العودة للبيانات</button>}<button className="message-close" onClick={()=>{setError('');setNotice('')}} aria-label="إغلاق"><X size={15}/></button></div>}

    <div className={`overview-layout ${run?'has-run':''}`}>
     {run&&<section className="scan-strip"><div className="scan-strip-head"><div><span className={`run-state ${run.status}`}>{run.status==='complete'?'مكتمل':run.status==='failed'?'فشل':resumable?'قيد التنفيذ':'جزئي'}</span><b>{run.source?.includes('quick')?'الفحص السريع':'فحص السوق الكامل'}</b><small>{stageLabels[run.stage]||`المرحلة ${run.stage}`}</small></div><small className="scan-date" dir="ltr">{new Date(run.created_at).toLocaleString('en-GB')}</small></div><div className="scan-main-number"><b>{(run.offset||0).toLocaleString('en-US')}</b><span>من {(run.total||0).toLocaleString('en-US')} شركة تم تقييمها</span></div><Progress value={progress}/><div className="progress-caption"><span>{progress.toFixed(1)}%</span><span>{resumable?'الفحص مستمر على الخادم ويمكنك إغلاق التطبيق.':'النتائج محفوظة وجاهزة للمراجعة.'}</span></div><div className="scan-numbers"><span><b>{run.screened_out?.toLocaleString('en-US')||0}</b> استُبعدت مبكرًا</span><span className={run.failed||run.sec_failed?'negative':''}><b>{(run.failed||0)+(run.sec_failed||0)}</b> أخطاء</span></div></section>}
     <section className="metric-row">
      <Metric icon={Database} label="تم فحصها" value={radarSummary.total||run?.universe_total||0} meta="الكل" tone="neutral"/>
      <Metric icon={ShieldCheck} label="مقبولة" value={screeningQualified} meta="اجتازت كل البوابات" tone="positive"/>
      <Metric icon={Clock3} label="بيانات ناقصة" value={incomplete} meta="في مركز البيانات" tone="warning"/>
      <Metric icon={Layers} label="مستبعدة" value={excluded} meta="خارج القائمة" tone="negative"/>
     </section>
    </div>

    {tab==='home'&&!notificationsEnabled&&<section className="notification-panel"><span className="notification-panel-icon"><Bell size={20}/></span><div><b>أخبرني عند اكتمال الفحص</b><p>{notificationState==='denied'?'الإشعارات محظورة. فعّلها من إعدادات الهاتف ثم افتح التطبيق مجددًا.':notificationState==='unsupported'?'على iPhone ثبّت التطبيق أولًا وافتحه من الشاشة الرئيسية.':'فعّل الإشعارات مرة واحدة، ويمكنك بعدها إغلاق التطبيق بأمان.'}</p></div><button onClick={notificationState==='unsupported'?()=>setInstallHelp(true):enableNotifications}>{notificationState==='unsupported'?'طريقة التثبيت':notificationState==='denied'?'التعليمات':'تفعيل الإشعارات'}</button></section>}
    {tab==='home'&&notificationsEnabled&&<section className="notification-panel enabled"><span className="notification-panel-icon"><Check size={20}/></span><div><b>الإشعارات مفعّلة</b><p>سنرسل لك تنبيهًا فور انتهاء الفحص.</p></div><button onClick={enableNotifications}>اختبار</button></section>}

    {tab==='lab'?<LabPanel lab={lab} setLab={setLab}/>:tab==='data'?<DataPanel run={run} exportData={exportData} importFile={importFile}/>:<>
     <div className="mobile-filters" aria-label="قوائم الرادار"><button className={strategy==='bounce'&&tab!=='favorites'?'active bounce':''} onClick={()=>switchTab('bounce')}>فرص الارتداد {mobileBounceCount?`(${mobileBounceCount})`:''}</button><button className={strategy==='core'&&tab!=='favorites'?'active core':''} onClick={()=>switchTab('core')}>القيمة الأساسية {mobileCoreCount?`(${mobileCoreCount})`:''}</button><button className={tab==='favorites'?'active favorite':''} onClick={()=>switchTab('favorites')}>قائمة المتابعة {favorites.length?`(${favorites.length})`:''}</button></div>
     <section className={`mobile-strategy-brief ${strategy}`}><p>{strategy==='bounce'?'هدف مختلف عن بقية القوائم: احتمال أن يلمس السهم +20% قريبًا، لا أن يتضاعف. الملف هنا معكوس — أسهم مهزومة هبطت أكثر من 35% خلال سنة وبدأت ترتد من قاعها.':'شركات قوية الأساسيات وسعرها بالأرض — رخيصة على المبيعات والدفتري والربح التشغيلي معًا، ورابحة، ومنضبطة في إصدار الأسهم. أفق هذه الفئة 12–24 شهرًا لا أسابيع: إعادة التسعير بطيئة بطبيعتها.'}</p><div className="mobile-brief-stats"><div><b>{strategy==='bounce'?'65.6%':'+23%'}</b><span>{strategy==='bounce'?'تبلغ +20% خلال 6 أشهر':'وسيط العائد 24 شهرًا'}</span></div><div><b>{strategy==='bounce'?'43.9%':'+3%'}</b><span>{strategy==='bounce'?'السوق لنفس الهدف':'وسيط السوق'}</span></div><div><b>{strategy==='bounce'?'28':'25%'}</b><span>{strategy==='bounce'?'وسيط أيام البلوغ':'انهيار مقابل السوق'}</span></div></div><button>▸ {strategy==='bounce'?'الأدلة الكاملة والمخاطر':'كيف بُنيت وما حدودها'}</button></section>
     <section className="strategy-switch" aria-label="اختيار الاستراتيجية"><button className={strategy==='core'?'active':''} onClick={()=>switchTab('core')}><span><ShieldCheck size={18}/><b>القيمة الأساسية</b></span><small>استثمار 12–24 شهرًا</small></button><button className={strategy==='bounce'?'active':''} onClick={()=>switchTab('bounce')}><span><Activity size={18}/><b>فرص الارتداد</b></span><small>هدف قصير حتى 3 أشهر</small></button><div className="strategy-note"><Info size={16}/><span>تظهر هنا فقط الشركات التي اجتازت جميع بوابات القائمة المختارة.</span></div></section>

     <section className="research-panel"><div className="panel-heading"><div><span className="section-eyebrow">{tab==='favorites'?'قائمة المتابعة':'نتائج مؤهلة'}</span><h2>{tab==='favorites'?'المفضلة':strategy==='core'?'القيمة الأساسية':'فرص الارتداد'} <em>{rows.length}</em></h2><p>{tab==='favorites'?'الشركات التي حفظتها للمتابعة السريعة.':'شركات اجتازت جميع بوابات الاستراتيجية؛ افتح أي بطاقة لمراجعة الدرجة من 100 والأدلة.'}</p></div><button className="ghost-button" onClick={exportData}><Download size={16}/> تصدير</button></div><div className="control-row"><label className="search-field"><Search size={17}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="ابحث بالاسم أو الرمز" aria-label="بحث الشركات"/></label></div>
      {rows.length?<StockTable rows={rows} strategy={strategy} favorites={favorites} onSelect={openCompany} onFavorite={favorite}/>:<EmptyState hasData={!!data.length} incomplete={incomplete} onQuick={()=>scan('quick')} onDemo={()=>{setData(fixtures);setDemo(true);setRun(null);setError('');setNotice('')}}/>}
     </section>
    </>}
    <footer className="app-footer"><span>رادار الشركات الصغيرة V2</span><span>بيانات مؤرخة · مصادر معلنة · دون نتائج مصطنعة</span></footer>
   </div>
  </main>

  <nav className="bottom-nav" aria-label="التنقل الرئيسي">{navItems.slice(0,4).map(([id,name,Icon])=><button key={id} className={tab===id?'active':''} onClick={()=>switchTab(id)}><Icon size={20}/><span>{name.replace('لوحة الفرص','الفرص').replace('القيمة الأساسية','القيمة').replace('فرص الارتداد','الارتداد').replace('قائمة المتابعة','المتابعة')}</span></button>)}<button className={tab==='lab'||tab==='data'?'active':''} onClick={()=>setMobileNav(true)}><Menu size={20}/><span>المزيد</span></button></nav>

  <CompanyDialog selected={selected} strategy={strategy} loading={detailLoading} onClose={()=>setSelected(null)}/>
  <Dialog open={installHelp} onOpenChange={setInstallHelp}><DialogContent dir="rtl" className="install-dialog"><DialogTitle>تثبيت رادار على الهاتف</DialogTitle><DialogDescription>افتح التطبيق من هاتفك وأنت مسجل بالحساب نفسه.</DialogDescription><div className="install-step"><b>iPhone / Safari</b><span>مشاركة ← إضافة إلى الشاشة الرئيسية</span></div><div className="install-step"><b>Android / Chrome</b><span>القائمة ⋮ ← تثبيت التطبيق</span></div></DialogContent></Dialog>
 </div>
}

function Metric({icon:Icon,label,value,meta,tone}:{icon:any,label:string,value:number,meta:string,tone:string}){return <div className={`metric ${tone}`}><span className="metric-icon"><Icon size={18}/></span><div><small>{label}</small><b>{value.toLocaleString('en-US')}</b><span>{meta}</span></div></div>}

function StockTable({rows,strategy,favorites,onSelect,onFavorite}:{rows:any[];strategy:'core'|'bounce';favorites:string[];onSelect:(s:Snapshot)=>void;onFavorite:(symbol:string)=>void}){return <div className="table-wrap"><div className="table-head"><span>الشركة</span><span>السعر</span><span>القيمة السوقية</span><span>12 شهرًا</span><span>الدرجة</span><span>الحالة</span><span>جودة البيانات</span><span/></div>{rows.map(({s,e})=>{const screening=(e as any).screeningQualified;const badge=screening?'مقبول':labels[e.status];const score=e.score??e.gateScore;const displayScore=score==null?null:Number(score);return <div className={`table-row ${screening?'approved':e.status.toLowerCase()}`} key={s.symbol}><button className="issuer" onClick={()=>onSelect(s)}><span className="ticker">{s.symbol.slice(0,2)}</span><span><b dir="ltr">{s.symbol}</b><small>{s.name}</small></span></button><button className="copy-symbol" onClick={()=>void navigator.clipboard?.writeText(s.symbol)} aria-label={`نسخ رمز ${s.symbol}`}>نسخ الرمز</button><button className="cell-number" onClick={()=>onSelect(s)}><b>${money(s.price)}</b></button><button className="cell-number" onClick={()=>onSelect(s)}><b>${money(s.marketCap)}</b></button><button className={`cell-number ${s.return12m!=null&&(s.return12m<0?'down':'up')}`} onClick={()=>onSelect(s)}><b>{pct(s.return12m)}</b></button><button className="cell-number score-cell" onClick={()=>onSelect(s)}><b>{displayScore!=null?displayScore.toFixed(1):'—'}</b><small>{e.score!=null?(e.scoreCoverage<100?`${e.scoreCoverage}% بيانات`:'من 100'):e.gateScore!=null?'اكتمال البوابات':'غير متاح'} · {oldScoreLabel(displayScore,strategy)}</small></button><button className="status-cell" onClick={()=>onSelect(s)}><span className={`result-badge ${screening?'pass':e.status.toLowerCase()}`}>{badge}</span><small>{screening?'كل البوابات مكتملة':e.status==='PASS'?'كل الشروط مكتملة':e.checks.find((c:any)=>c.status===e.status)?.label||'راجع تفاصيل الفحص'}</small></button><button className="quality-cell" onClick={()=>onSelect(s)}><b>{s.confidence||'F'}</b><small>{strategy==='core'?'درجة قابلة للتفسير':'بوابات موثقة'}</small></button><div className="mobile-checks">{e.checks.filter((check:any)=>!['provenance','freshness','filingFreshness','conflict'].includes(check.id)).slice(0,6).map((check:any)=><span key={check.id} className={check.status.toLowerCase()}>{checkMark(check.status)} {oldCheckLabels[check.id]||check.label}</span>)}</div><button className={`watch ${favorites.includes(s.symbol)?'saved':''}`} aria-label="حفظ في قائمة المتابعة" onClick={()=>onFavorite(s.symbol)}><Star size={17}/></button></div>})}</div>}

function EmptyState({hasData,incomplete,onQuick,onDemo}:{hasData:boolean;incomplete:number;onQuick:()=>void;onDemo:()=>void}){return <div className="empty-state"><div className="empty-rule"/><span className="section-eyebrow">{hasData?'نتيجة الفحص الحالي':'لا توجد لقطة بعد'}</span><h3>{hasData?'لا توجد شركات اجتازت كل البوابات':'لم تُنشأ لقطة سوق بعد'}</h3><p>{hasData?`${incomplete.toLocaleString('ar')} شركة لديها بيانات ناقصة؛ لم تُدرج في القوائم الرئيسية.`:'ابدأ بفحص سريع، وستظهر النتائج تباعًا مع حفظ التقدم.'}</p>{!hasData&&<div><button className="scan-primary" onClick={onQuick}>تشغيل فحص سريع</button><button className="text-button" onClick={onDemo}>بيانات توضيحية</button></div>}</div>}

function LabPanel({lab,setLab}:{lab:any;setLab:(v:any)=>void}){return <section className="research-panel content-panel"><div className="panel-heading"><div><span className="section-eyebrow">VALIDATION HARNESS</span><h2>مختبر الاستراتيجية</h2></div><span className="result-badge unknown">بحث قابل لإعادة الإنتاج</span></div><div className="analysis-grid"><article><div className="article-icon"><FlaskConical size={19}/></div><h3>محاكاة خروج محافظة</h3><p>إذا لامس السعر الهدف ووقف الخسارة في الجلسة نفسها، يفترض المحرك تنفيذ الوقف أولًا. يشمل المثال انزلاقًا سعريًا قدره 50 نقطة أساس.</p><button className="ghost-button" onClick={()=>setLab(simulateExit(100,'2026-01-01',[{date:'2026-01-02',open:100,high:122,low:83,close:110}],{slippageBps:50,commission:0,shares:1}))}>تشغيل المحاكاة</button>{lab&&<div className="analysis-result"><span>النتيجة</span><b>{lab.reason==='stop'?'وقف الخسارة':'خروج'} · {pct(lab.netReturn)}</b></div>}</article><article><div className="article-icon"><ShieldCheck size={19}/></div><h3>بوابات الاعتماد</h3>{['استخدام تاريخ توفر الإفصاح الفعلي','عزل الشركات في عينات الاختبار','تصحيح تعدد التجارب','إضافة تكاليف التداول والانزلاق','إظهار تحيز الشركات المشطوبة'].map(item=><div className="validation-line" key={item}><Check size={15}/><span>{item}</span></div>)}<a href="/api/export?kind=spec" className="inline-link">تنزيل مواصفات المحرك <ArrowUpLeft size={14}/></a></article></div><div className="method-note"><Info size={17}/><div><b>حدود الاعتماد</b><p>تعريف الانعطاف إلى الربحية ودرجات العوامل الفرعية لم يَرِد بمعادلات قابلة لإعادة الإنتاج. لذلك تُعرض البوابات وحالة البيانات دون صناعة درجة استثمارية وهمية.</p></div></div></section>}

function DataPanel({run,exportData,importFile}:{run:any;exportData:()=>void;importFile:(f?:File)=>void}){return <section className="research-panel content-panel"><div className="panel-heading"><div><span className="section-eyebrow">مصادر البيانات</span><h2>مركز البيانات</h2></div><button className="ghost-button" onClick={exportData}><Download size={16}/> سجل التدقيق</button></div>{run&&<div className="coverage-grid"><div><span>تغطية الأسعار</span><b>{run.quote_coverage?.toLocaleString('en-US')||0}</b><small>من {run.universe_total?.toLocaleString('en-US')||0}</small></div><div><span>مجموعات SEC Frames</span><b>{run.sec_success||0}/{run.sec_requests||0}</b><small>{run.sec_failed||0} غير متاحة</small></div><div><span>تغطية الأساسيات</span><b>{run.fundamental_coverage?.toLocaleString('en-US')||0}</b><small>شركة</small></div><div><span>المرحلة الحالية</span><b>{run.stage||0}</b><small>{stageLabels[run.stage]||'—'}</small></div></div>}<div className="source-grid"><article><div className="source-head"><span><Wifi size={18}/></span><div><b>Yahoo + Nasdaq</b><small>Bulk quotes · 250 رمزًا لكل طلب</small></div><span className="source-state"><span className="live-dot"/> جماعي</span></div><p>يُسعّر السوق في دفعات كبيرة، مع Nasdaq واللقطة المؤرخة كمسار احتياطي، بدل طلب تاريخ مستقل لكل شركة.</p></article><article><div className="source-head"><span><Server size={18}/></span><div><b>SEC EDGAR</b><small>XBRL Frames · 13 مجموعة رسمية</small></div><span className="source-state review">رسمي</span></div><p>تُجمع الأساسيات لآلاف الشركات حسب المقياس والفترة، مع لقطة رسمية مؤرخة عند حجب واجهة SEC للخادم. ينتقل Company Facts والتاريخ التفصيلي إلى التحقق عند فتح المرشح.</p></article></div><div className="data-actions"><div><h3>استيراد لقطة موثقة</h3><p>JSON من نوع Snapshot[] مع المصدر وتاريخ التوفر لكل مقياس.</p></div><label className="ghost-button upload"><FileUp size={16}/> اختيار ملف<input type="file" accept="application/json,.json" onChange={event=>importFile(event.target.files?.[0])}/></label><a className="inline-link" href="/api/export?kind=schema">قاموس البيانات</a></div>{run?.error&&<div className="system-message danger"><AlertTriangle size={17}/>{run.error}</div>}</section>}

function statusText(status:string){return status==='PASS'?'اجتاز':status==='FAIL'?'فشل':'ناقص'}
function SectorWarning({sector,industry}:{sector?:string;industry?:string}){const text=`${sector||''} ${industry||''}`;const match=/biotech|pharmaceutic|drug|life scien/i.test(text)?['تقنية حيوية / أدوية',46.2]:/software|internet|information tech/i.test(text)?['برمجيات',33.5]:/semiconduct|electronic/i.test(text)?['أشباه موصلات',30]:/bank|financ|insur/i.test(text)?['بنوك ومالية',8.3]:null;if(!match)return null;const [label,rate]=match;const high=Number(rate)>=30;return <p className={`sector-warning ${high?'high':'low'}`}>قطاع {label}: تاريخياً {rate}% من الشركات فيه خسرت أكثر من نصف قيمتها خلال سنتين. {high?'تشتت عالٍ — صغّر حجم المركز.':'تشتت منخفض.'}<br/><span>هذه معلومة تحذيرية ولا تدخل في الدرجة — القطاع الأفضل يتبدّل كل سنة، فلا يصلح معياراً.</span></p>}
function RangeBar({snapshot}:{snapshot:Snapshot}){if(!(snapshot.price&&snapshot.low52w&&snapshot.high52w&&snapshot.high52w>snapshot.low52w))return <div className="unavailable-note">نطاق 52 أسبوعًا غير متاح من مصدر موثق.</div>;const position=Math.max(0,Math.min(100,(snapshot.price-snapshot.low52w)/(snapshot.high52w-snapshot.low52w)*100));return <section className="range-section"><div className="detail-section-heading"><span>الموقع في نطاق 52 أسبوعاً</span><b>{position.toFixed(0)}%</b></div><div className="range-track"><i style={{right:`${position}%`}}/></div><div className="range-labels"><span>{snapshot.high52w.toFixed(2)} أعلى</span><span>أدنى {snapshot.low52w.toFixed(2)}</span></div></section>}
function EarningsExtras({snapshot}:{snapshot:Snapshot}){const surprises=snapshot.surprises||[];return <><section className="earnings-grid"><div><small>التقرير القادم</small><b>{snapshot.nextEarnings||'غير معلن'}</b><span>{snapshot.nextEarnings?'موعد متاح من المصدر':'لا يوجد موعد موثق'}</span></div><div><small>آخر تقرير</small><b className={snapshot.lastEarningsStatus==='إيجابي'?'good':snapshot.lastEarningsStatus==='سلبي'?'bad':''}>{snapshot.lastEarningsStatus||'غير معروف'}</b><span>{snapshot.lastEarnings||'تاريخ غير متاح'}</span></div></section>{surprises.length>0?<section className="surprises"><h3 className="dialog-section-title">مفاجآت الأرباح (الفعلي مقابل المتوقع)</h3><div className="surprise-grid">{surprises.slice(-4).map(item=><div key={item.quarter}><b className={(item.surprisePct??0)>=0?'good':'bad'}>{item.surprisePct==null?'—':`${item.surprisePct>=0?'+':''}${item.surprisePct.toFixed(0)}%`}</b><span>{item.quarter}</span></div>)}</div></section>:<div className="unavailable-note">مفاجآت الأرباح غير متاحة من مصدر موثق لهذه الشركة.</div>}</>}
function RevenueTrend({rows}:{rows:NonNullable<Snapshot['revenueTrend']>}){if(rows.length<2)return null;const max=Math.max(...rows.map(item=>item.value),1);return <section className="revenue-trend"><h3 className="dialog-section-title">الإيرادات الربعية</h3><div className="revenue-bars">{rows.slice(-6).map(item=><div className="revenue-bar" key={item.quarter}><div><i style={{height:`${Math.max(6,item.value/max*100)}%`}} title={`$${money(item.value)}`}/></div><span>{item.quarter}</span><small>${money(item.value)}</small></div>)}</div></section>}
function CompanyDialog({selected,strategy,loading,onClose}:{selected:Snapshot|null;strategy:'core'|'bounce';loading:boolean;onClose:()=>void}){const evaluation=selected?evaluateStrategy(strategy,selected):null;const longTerm=selected?evaluateStrategy('core',selected):null;const displayScore=evaluation?.score??evaluation?.gateScore??null;return <Sheet open={!!selected} onOpenChange={open=>!open&&onClose()}><SheetContent side="bottom" showCloseButton={false} className="company-sheet" dir="rtl"><div className="detail-handle"/><div className="detail-sticky-head"><div><SheetTitle>{selected?.symbol} · {selected?.name}</SheetTitle><SheetDescription>{loading?'جارٍ جلب التاريخ وCompany Facts والتحليل العميق…':`تفاصيل الفحص والأدلة — ${strategy==='core'?'القيمة الأساسية':'فرص الارتداد'}`}</SheetDescription></div><button className="detail-close" onClick={onClose}>إغلاق ✕</button></div>{selected&&evaluation&&<div className="company-detail-body"><div className="detail-identity"><div><b>${money(selected.price)}</b><span className={selected.return12m!=null&&selected.return12m<0?'down':'up'}>{pct(selected.return12m)} خلال 12 شهرًا</span></div><div><small>{selected.exchange||'السوق غير متاح'}</small><b>${money(selected.marketCap)}</b><span>{selected.sector||'القطاع غير متاح'}</span></div></div><section className={`detail-score ${strategy}`}><div><small>{strategy==='bounce'?'درجة الارتداد':'الدرجة من 100'}</small><strong>{displayScore==null?'—':Number(displayScore).toFixed(1)}</strong><span>{oldScoreLabel(displayScore,strategy)}</span></div><div><b>{strategy==='bounce'?'بوابات قبول فعلية':'تغطية الدرجة'}</b><span>{strategy==='bounce'?`${evaluation.checks.filter((check:any)=>check.id!=='provenance'&&check.id!=='freshness'&&check.id!=='conflict'&&check.status==='PASS').length} بوابة ناجحة`:`${evaluation.scoreCoverage||0}% من الأوزان متاحة`}</span></div>{strategy==='bounce'&&<div><b>طويل المدى</b><span>{longTerm?.score==null?'غير متاح':Number(longTerm.score).toFixed(1)}</span></div>}</section>{strategy==='bounce'&&<p className="bounce-detail-copy">الهدف: بيع عند <b>+20%</b> · وقف عند <b>−15%</b> · خروج بعد 3 أشهر إن لم يتحرك. هذه الدرجة تقيس اكتمال بوابات الارتداد، وليست احتمال ربح تنبؤيًا.</p>}<div className="detail-metrics"><div><small>السوق</small><b>{selected.exchange||'غير متاح'}</b></div><div><small>الصناعة</small><b>{selected.industry||'غير متاح'}</b></div><div><small>نوع السهم</small><b>{selected.securityType==='common'?'سهم عادي':'غير متاح'}</b></div></div><div className="company-description"><h3>ماذا تفعل الشركة</h3><p>{selected.descriptionAr||selected.description||'الوصف غير متاح من مصدر موثق لهذه اللقطة.'}</p><SectorWarning sector={selected.sector} industry={selected.industry}/><small>{[selected.industry,selected.employees?`${selected.employees.toLocaleString('en-US')} موظف`:null].filter(Boolean).join(' · ')}</small></div><div className="company-financials"><div><small>الإيرادات</small><b>${money(selected.revenue)}</b></div><div><small>صافي الدخل</small><b>${money(selected.netIncome)}</b></div><div><small>التدفق النقدي الحر</small><b>${money(selected.fcf)}</b></div><div><small>EV/S</small><b>{selected.evSales==null?'—':selected.evSales.toFixed(2)}</b></div><div><small>P/S</small><b>{selected.ps==null?'—':selected.ps.toFixed(2)}</b></div><div><small>النقد / الدين</small><b>${money(selected.cash)} / ${money(selected.debt)}</b></div><div><small>السيولة اليومية</small><b>${money(selected.medianDollarVolume20d)}</b></div><div><small>التخفيف</small><b>{pct(selected.dilution)}</b></div></div><PriceChart snapshot={selected} bounce={strategy==='bounce'}/>{selected.dataIssues?.map(issue=><div className="system-message danger" key={issue}><AlertTriangle size={16}/>{issue}</div>)}{strategy==='core'&&evaluation.factors?.length>0&&<><h3 className="dialog-section-title">تفصيل الدرجة من 100</h3><div className="factor-list">{evaluation.factors.map((factor:any)=><div className="factor-row" key={factor.id}><div><b>{oldFactorLabels[factor.id]||factor.label}</b><small>{factor.available?factor.explanation:'البيانات غير متاحة — لم تُحتسب نقاطًا'}</small></div><strong>{factor.points.toFixed(1)} / {factor.maxPoints}</strong></div>)}</div></>}{strategy==='bounce'&&<div className="trade-plan"><span>خطة الارتداد</span><b>هدف {selected.price?`$${money(selected.price*1.2)}`:'غير متاح'}</b><b>وقف {selected.price?`$${money(selected.price*.85)}`:'غير متاح'}</b><b>3 أشهر كحد أقصى</b></div>}<h3 className="dialog-section-title">معايير القرار والبوابات</h3><div className="gate-list">{evaluation.checks.map((check:any)=><div className="gate-row" key={check.id}><span className={`gate-dot ${check.status.toLowerCase()}`}/><div><b>{checkMark(check.status)} {oldCheckLabels[check.id]||check.label}</b><small>{check.explanation}</small></div><span className={`result-badge ${check.status.toLowerCase()}`}>{statusText(check.status)}</span></div>)}</div><EarningsExtras snapshot={selected}/><div className="analyst-card"><b>متوسط أهداف المحللين</b><span>{(selected.targetMean??selected.analystTarget)==null?'غير متاح من مصدر موثق':`$${money(selected.targetMean??selected.analystTarget)}${selected.targetLow!=null&&selected.targetHigh!=null?` · النطاق ${money(selected.targetLow)}–${money(selected.targetHigh)}`:''}${selected.analystCount?` · ${selected.analystCount} محلل`:''}`}</span><small>هذا رأي المحللين وليس «القيمة العادلة». لا تُصنع الأهداف عند غياب المصدر.</small></div><RangeBar snapshot={selected}/>{selected.backlog?.amount!=null&&<div className="analyst-card"><b>صبر الطلبات (الإيراد المتعاقد عليه)</b><span>${money(selected.backlog.amount)} · {dateOnly(selected.backlog.asOf)}</span></div>}{selected.news?.length?<><h3 className="dialog-section-title">أحدث الأخبار</h3><div className="news-list">{selected.news.map((item:any)=><a className="news-item" href={item.link||'#'} target="_blank" rel="noreferrer" key={`${item.title}-${item.publishedAt}`}><b>{item.title}</b><small>{item.publishedAt||'تاريخ غير متاح'} · {item.source||'مصدر غير متاح'}</small></a>)}</div></>:<div className="unavailable-note">لا توجد أخبار حديثة متاحة من مصدر موثق.</div>}{selected.insiderPurchases?.length?<div className="insider-panel"><h4>مشتريات المطلعين — SEC Form 4 P</h4>{selected.insiderPurchases.slice(0,6).map(purchase=><a href={purchase.source||'#'} target="_blank" rel="noreferrer" key={`${purchase.owner}-${purchase.date}-${purchase.value}`}><span><b>{purchase.owner}</b><small>{purchase.date} · {purchase.shares.toLocaleString('en-US')} سهم</small></span><strong>${money(purchase.value)}</strong></a>)}</div>:<div className="unavailable-note">لا توجد مشتريات مفتوحة Form 4 برمز P متاحة لهذه اللقطة.</div>}<RevenueTrend rows={selected.revenueTrend||[]}/><h3 className="dialog-section-title">مصادر الأرقام وسجل التحقق</h3><div className="evidence-list">{Object.entries(selected.provenance||{}).map(([key,p]:any)=><div className="evidence-row" key={key}><div><b>{key}</b><small>{p.source}</small></div><span>{p.periodEnd||'غير متاح'}</span>{p.url?<a href={p.url} target="_blank" rel="noreferrer">فتح المصدر ↗</a>:<span>—</span>}</div>)}</div><div className="research-completeness"><span>اكتمال زوايا البحث</span><b>{Object.values(selected.research||{}).filter(Boolean).length}/4</b><small>UNKNOWN لا يساوي PASS، ولا تدخل البيانات الناقصة قائمة القبول.</small></div></div>}</SheetContent></Sheet>}
