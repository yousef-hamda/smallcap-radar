export async function saveOffline(value:unknown){if(!('indexedDB'in window))return;const database=await new Promise<IDBDatabase>((resolve,reject)=>{const request=indexedDB.open('radar-offline',1);request.onupgradeneeded=()=>request.result.createObjectStore('snapshots');request.onsuccess=()=>resolve(request.result);request.onerror=()=>reject(request.error)});await new Promise<void>((resolve,reject)=>{const t=database.transaction('snapshots','readwrite');t.objectStore('snapshots').put(value,'latest');t.oncomplete=()=>resolve();t.onerror=()=>reject(t.error)});database.close();}
