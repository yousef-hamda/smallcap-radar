# إعادة البناء — 8 سبتمبر 2026

## تشخيص قبل تعديل المصدر

المرجع: الصور IMG_4215–IMG_4224، الموقع القديم المفتوح مباشرة، والكود المنشور index-B5KnXmO3.pretty.js. فحص البداية: typecheck وlint و39 اختبار محرك و6 اختبارات مكونات وproduction build ناجحة. نجاحها لم يكشف الأخطاء التالية، ولذلك لا يكفي للقبول.

- جدول watchlist مشترك بين كل الزوار؛ العدد لا يثبت أن المستخدم أضاف سهمًا. لا ننقل هذه الصفوف إلى قائمة شخصية ولا نحذف بيانات مشتركة مجهولة الملكية.
- stage9 يعيد done=true عند الانتقال إلى stage10، فيوقف العامل ويرسل إشعارًا قبل التاريخ. العميل كان يدفع خطوات POST إضافية، ما أخفى هذا العطل.
- processed يُصفّر عند نهاية التقييم ولا يتحدث في التاريخ؛ readState يستبعد النتيجة المكتملة إن processed=0.
- offset/total ليس تقدمًا إجماليًا، ويعود للصفر بين المراحل. المطلوب نسبة عمل موثقة بمرحلتها، ليست تقديرًا للزمن.
- التاريخ يجلب 1900 يوم لكل مرشح. متوسط حجم عشرة أيام يستبعد بعض المرشحين رغم أن معيار الاستراتيجية وسيط عشرين جلسة؛ الاستبعاد بهذا البديل غير مكافئ.
- CSS يحتوي تصميمًا أزرق وقائمة جانبية ثم بدائل هاتفية ثم بدائل إضافية؛ الشريط السفلي والدرج لا يملكان نموذج تخطيط واحدًا.
- الرسم يخفي أزرار الفترة عند عدم وجود بيانات. هدف Nasdaq لسنة واحدة يُسمّى خطأً متوسط المحللين. محلل الأرقام يحوّل النص الفارغ إلى صفر.

## ما أثبته المرجع وما لم يثبته

الواجهة القديمة تستدعي مراحل quotes ثم frames ثم scoring ثم confirming ثم logging ثم resolving، وتطلب قوائم بصفحات 40 سهمًا. ملف الشركة يستدعي إثراءً منفصلًا. المفضلة القديمة خاصة بالمتصفح. الكود المتاح لا يكشف تنفيذ الخادم أو صيغة درجة Bounce كاملة؛ لا يمكن إثبات زمن أقل من دقيقة لجميع هذه العمليات من الصور. المشاهدة الحية أظهرت مؤقتًا قائمة مؤهلة ببيانات القائمة السابقة أثناء تحميلها، فلا ننسخ سباق الطلبات هذا.

ألوان المصدر القديم: zinc-950 للخلفية، zinc-900/50 للبطاقات، emerald للفحص والحالات الناجحة، sky للارتداد، violet للقيمة، amber للمفضلة. لا يوجد شريط تنقل سفلي في المرجع؛ يوجد زر إغلاق أبيض داخل درج الشركة. الوجهات النهائية ثلاث حسب طلب المستخدم، لا مؤهلة/مستبعدة كوجهات رئيسية.

## قبول الإصدار

لا ادعاء تطابق كامل؛ المقاييس القديمة 65.6%/43.9%/28 و+23%/+3%/25% مراجع تاريخية غير معاد إنتاجها وليست نتائج حية. لا يساوي اكتمال بوابات Bounce درجة تنبؤية قديمة.

## التغييرات المكتوبة

- حذف page.tsx وglobals.css وprice-chart.tsx السابقين وإعادة إنشائها؛ إزالة القائمة الجانبية والشريط السفلي المتضارب. الدرجة والبطاقات والدرج تستهلك بيانات حقيقية فقط.
- درج واحد بثلاث مناطق: رأس ثابت، محتوى قابل للتمرير، زر إغلاق داخل حدود الدرج مع safe-area. لم يُختبر موقعه بصريًا بعد.
- progress 0.00–100.00: نسب مراحل تشغيلية 0/20/35/60 ثم أقل من 100 أثناء التاريخ؛ 100 فقط للحالة complete بمرحلة نهائية. ليست ETA. الاختبار يثبت عدم التراجع عند حدود المراحل.
- تصحيح done=false في الانتقال 9→10، وعدم تصفير processed؛ وإضافة stage إلى شرط حجز lease لمنع تكرار دفعة قديمة بعد انتقال المرحلة.
- اختيار النتائج المكتملة ذات الصفوف الفعلية حتى لو كانت الجولة القديمة تحمل processed=0. النتيجة الجزئية لا تستبدل نتيجة السوق المكتملة.
- مفضلة خاصة مخزنة في D1 بهوية المنصة أو cookie عشوائي HttpOnly للزائر. الكتابة صريحة saved=true/false وقابلة للتكرار بأمان. القائمة المشتركة السابقة محفوظة ولم تُنسب لأي مستخدم جديد.
- migration 0003 تضيف personal_watchlist فقط. كانت أعمدة strategy_runs وbulk_fundamentals موجودة بواسطة التهيئة القديمة؛ أزيلت من SQL الجديدة غير المطبقة كي لا تعيد إنشاءها. metadata الجديدة تعكس تعريف schema الحالي. لم تُعدّل أي migration مطبقة.
- التاريخ 450 يومًا للفحص و1900 للتفاصيل، cache يومي مؤقت في D1 للفحص، 24 شركة في الدفعة. أزيل رفض المرشحين اعتمادًا على متوسط حجم عشرة أيام. لم يُقَس زمن الشبكة الكامل بعد.
- parser لا يحوّل الفراغ إلى صفر؛ تعارض المصادر يمنع screeningQualified؛ Core يأخذ الأوزان من SPECS.
- لا يُسمى Nasdaq OneYrTarget متوسط محللين. أضيفت مصادر حقول Yahoo، وحُفظت القيم المالية القديمة ذات المصدر عند فقد البديل؛ لا ترفع المقارنة بين مسارين من SEC ثقة المصدرين المستقلين.
- الرسم لا يخفي أزرار الفترات عند غياب التاريخ، يرشح التكرار وغير المنتهي والمستقبلي، ويعرض أدنى/أعلى حقيقيين. يوم التداول اللحظي غير متاح ويُذكر صراحة.

## أدلة الاختبار

| الفحص | النتيجة |
|---|---|
| typecheck وlint وproduction build | نجحت بعد إعادة البناء |
| المحرك | 39/39 |
| تكامل scanner/storage/API باستخدام SQLite مع مزود أعطال معزول | 13/13 |
| المكونات والـSSR وطلب Web Push المشفر | 9/9 |
| SQLite/rollback/lease السابقة | 3/3 |
| فحص بصري للنسخة السابقة والمرجع | أُجري قبل إزالة التصميم |
| فحص بصري للنسخة الجديدة | محجوب، لا نتيجة قبول |
| فحص كامل بالمزودين على الإنتاج بعد الإصلاح | لم يُنفّذ؛ لم تُنشر النسخة |
| وصول push على هاتف فعلي | غير مختبر |

قياسات محرك محلي فقط ببيانات اختبار اصطناعية: 2000 تقييم نحو 68ms، 10000 نحو 151ms، 15000 نحو 151ms بعد إحماء المحرك. هذه ليست قياسات لمسح السوق أو تحميل الواجهة أو سرعة المزودين، ولا تُستخدم كـETA.

## مانع النشر الحالي

بعد بدء agent preview وفحص النسخة السابقة، رفضت أداة المتصفح طلب فحص DOM للنسخة الجديدة: Browser URL policy blocks this action. لم نحاول تجاوز الرفض أو الوصول بعنوان/أداة بديلة. تشترط مهمة المستخدم اختبار الواجهة قبل النشر، لذلك يظل هذا التغيير مسودة مصدر ولا يستبدل الإصدار 59 المنشور. لا يوجد إجراء يمكن مطالبة المستخدم به لإلغاء القيد من داخل التطبيق.

## المتبقي الصريح

1. اختبار browser QA متاح ومعتمد: 360/430/768/1440، تفاعل الرسم، درج طويل، حالات الخطأ، البحث والمفضلة، والـsafe-area؛ المقارنة بالصور لا تثبتها اختبارات SSR.
2. اختبار عامل الإنتاج باستقلال العميل، توقف المزود واستئناف العامل وازدواج البدء؛ سيناريو stage9 مُثبت محليًا لكن استمرار الشبكة بعد الإغلاق غير مثبت لهذا الإصدار.
3. إصلاح أوسع مطلوب: restart في مرحلة universe، قفل عام يمنع full/quick المتزامنين، retry دائم لتاريخ المرشحين، أمان/عزل push، وعدّ SEC live/fallback دون دمج نسب النجاح.
4. طبقة درجات Legacy/Core/Bounce ليست نسخة حسابية مثبتة عن الخادم القديم؛ معايير normalization الأصلية وبيانات backtest وForm 4 الشاملة والتغطية الثانوية لا تزال فجوات.
5. الرسم اللحظي، الترجمة العربية الموثوقة، ونصوص backlog عند غياب البيانات المنظمة غير مكتملة.
6. حفظ version من مصدر مدفوع، النشر، فحص الإنتاج وAPI، وقياس زمن حقيقي، بعد حل مانع الاختبار. لا وعد بأقل من دقيقة قبل ذلك.
